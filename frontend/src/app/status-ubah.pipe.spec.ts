import { StatusUbahPipe } from './status-ubah.pipe';

describe('StatusUbahPipe', () => {
  it('create an instance', () => {
    const pipe = new StatusUbahPipe();
    expect(pipe).toBeTruthy();
  });
});
